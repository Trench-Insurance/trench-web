const config = {
  content: ["./src/**/*.{html,js,svelte,ts}"],

  theme: {
    fontFamily: {
      'sans': ['Montserrat', 'system-ui']
    },
    extend: {},
  },

  plugins: [],
};

module.exports = config;
